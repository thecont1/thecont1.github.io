---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";

const posts = await getCollection("photography", ({ data }) => data.status === "published");
---

<Layout title="Photography">
  <section class="page-header">
    <h1>Photography</h1>
  </section>
  
  <div class="grid">
    {posts.map((post) => (
      <article class="card">
        <a href={`/photography/${post.slug}`}>
          {post.data.heroImage && <img src={post.data.heroImage} alt={post.data.title} />}
          <h2>{post.data.title}</h2>
        </a>
      </article>
    ))}
  </div>
</Layout>

<style>
  .page-header {
    padding: 8rem 6vw 4rem;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 2rem;
    padding: 0 6vw 4rem;
  }
  .card img {
    width: 100%;
    height: auto;
    aspect-ratio: 4/3;
    object-fit: cover;
  }
  .card a {
    text-decoration: none;
    color: inherit;
  }
</style>
